# Dev dans le navigateur : l'API est sur la même machine.
# Sur émulateur Android, laisser la valeur par défaut du code (10.0.2.2),
# qui pointe vers la machine hôte depuis la VM.
VITE_API_URL=http://localhost:3000/api/v1
